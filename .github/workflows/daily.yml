name: Daily SPY news summary

on:
  schedule:
    # Daily 8:00am US/Eastern.
    # NOTE: GitHub cron runs in UTC. 8:00am ET is 12:00 UTC during daylight saving time.
    # If you want it to remain exactly 8:00am ET year-round, adjust this seasonally (EST would be 13:00 UTC).
    - cron: "0 12 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: daily-spy-news
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r spy-daily-news/requirements.txt

      - name: Generate summary
        run: |
          python spy-daily-news/src/daily_spy_news.py

      - name: Commit summary (if changed)
        run: |
          if [ -z "$(git status --porcelain spy-daily-news/summaries/)" ]; then
            echo "No changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add spy-daily-news/summaries/
          git commit -m "Add daily SPY news summary"
          git push

